buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:7.4.2")
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
        // Больше никаких локальных maven-путей здесь! 
        // Это защищает от бесконечного цикла (StackOverflow).
    }
}
